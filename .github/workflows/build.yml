name: build

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2

      - name: Setup node environment
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install 🔧
        run: npm install

      - name: Lint 🧐
        run: npx eslint src

      - name: Test 🚨
        run: npm test

      - name: Build 🏗️
        run: npm run build
